import React from "react";
import { NextPage } from "next";

const AccountsPage: NextPage = () => {
  return (
    <main>
      <h1>
        on this page you will have the login form. Along with a button to allow
        new users to register
      </h1>
      {/* <button>{}</button> */}
    </main>
  );
};

export default AccountsPage;
