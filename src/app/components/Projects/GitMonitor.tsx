


const GitMonitor = ({className, project}:{
    className: string
    project: number
}) => {

    return (
        <div className={`${className} h-full w-full bg-gradient-to-br from-zinc-800 to-zinc-900`}>

        </div>
    )
}

export default GitMonitor